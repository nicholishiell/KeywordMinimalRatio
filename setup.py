from setuptools import setup

setup(
    name="KeywordMinimalRatio",
    version="1.0.0",
    description="Performs keyword extraction using the Minimal Ratio algorithm",
    author="RCS",
    author_email="its.rcs@carleton.ca",
    install_requires=[],
    packages=["KeywordMinimalRatio"],
)